# --- OpenAI ---
# Required for live LLM and image generation.
# If OPENAI_API_KEY is empty/missing, the app falls back to MOCK MODE
# (deterministic stub agents). All tests run in mock mode by default.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
OPENAI_ORCHESTRATOR_MODEL=gpt-5.4
OPENAI_JUDGE_MODEL=gpt-5.4
OPENAI_IMAGE_MODEL=gpt-image-2

# --- Feature flags ---
USE_RAG=true
USE_LLM_CACHE=true
ENABLE_IMAGE_GENERATION=true
PARALLEL_DOMAIN_AGENTS=true
ENABLE_RUN_JUDGE=false

# --- Multi-agent discussion ---
MAX_AGENT_DISCUSSION_ROUNDS=3
MAX_RETRIEVED_DOCS=5

# --- Tier 2 RAG (optional overrides; see app/rag_config.py) ---
# RAG_BASELINE_FINAL_K=8
# RAG_AGENT_ROUND1_FINAL_K=4
# RAG_ENABLE_AGENT_RAG=true
# RAG_ENABLE_DISAGREEMENT_RAG=true
# RAG_ENABLE_RED_TEAM_RAG=true
# RAG_ENABLE_ROUND3_NEW_RETRIEVAL=false
# RAG_MAX_FINAL_EVIDENCE_ITEMS=12

# --- Token budget ---
MAX_AGENT_INPUT_CHARS=6000
MAX_EVIDENCE_CHARS=2500
SELF_POSITION_MAX_CHARS=1000

# --- Storage ---
SQLITE_PATH=data/scenarios.sqlite
RAG_CHUNKS_PATH=data/rag_chunks.json
GENERATED_IMAGES_DIR=data/generated_images
